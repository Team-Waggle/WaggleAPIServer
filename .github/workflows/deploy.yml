name: Deploy to EC2

on:
  push:
    branches:
      - dev
  pull_request:
    types: [ closed ]
    branches:
      - main

jobs:

  # ----------------------
  # 1) COMMON BUILD STEP
  # ----------------------
  build:
    runs-on: ubuntu-latest

    outputs:
      jar-path: ${{ steps.build.outputs.jar-path }}

    steps:
      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Corretto 21
      - name: Setup JDK (Corretto 21)
        uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version: '21'

      # 3. Build JAR
      - name: Build JAR
        id: build
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test

          echo "=== List build/libs ==="
          ls -al build/libs

          # 최신 jar 자동 선택
          JAR_PATH=$(ls -t build/libs/*.jar | head -n 1)

          echo "JAR_PATH=$JAR_PATH"
          echo "jar-path=$JAR_PATH" >> $GITHUB_OUTPUT


  # ----------------------
  # 2) DEV DEPLOYMENT
  # ----------------------
  deploy-dev:
    needs: build
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest

    steps:
      # 1. Upload
      - name: Upload JAR to EC2 (DEV)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: ${{ needs.build.outputs.jar-path }}
          target: /opt/waggle-dev/app.jar
          overwrite: true

      # 2. Restart
      - name: Restart DEV service
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "======== DEV Deployment ========"
            sudo systemctl stop waggle-dev || true
            sudo systemctl daemon-reload
            sudo systemctl start waggle-dev
            sudo systemctl status waggle-dev --no-pager
            echo "======== DEV DONE ========"


  # ----------------------
  # 3) PROD DEPLOYMENT
  # ----------------------
  deploy-prod:
    needs: build
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'main'
    runs-on: ubuntu-latest

    steps:
      # 1. Upload
      - name: Upload JAR to EC2 (PROD)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: ${{ needs.build.outputs.jar-path }}
          target: /opt/waggle/app.jar
          overwrite: true

      # 2. Restart
      - name: Restart PROD service
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "======== PROD Deployment ========"
            sudo systemctl stop waggle || true
            sudo systemctl daemon-reload
            sudo systemctl start waggle
            sudo systemctl status waggle --no-pager
            echo "======== PROD DONE ========"
